'use client'
import Image from 'next/image'
import { useState } from 'react'
import { ChevronLeft, ChevronRight } from 'lucide-react'
export function PropertyCarousel({ images, name }: { images: string[]; name: string }) { const [index, setIndex] = useState(0); if (!images.length) return null; return <div className="relative bg-muted p-3"><div className="relative h-72 sm:h-[28rem]"><Image src={images[index]} alt={`${name} image ${index + 1}`} fill className="rounded-lg object-cover" priority /></div>{images.length > 1 && <><button aria-label="Previous image" onClick={() => setIndex((index - 1 + images.length) % images.length)} className="absolute left-6 top-1/2 rounded-full bg-background/80 p-2"><ChevronLeft /></button><button aria-label="Next image" onClick={() => setIndex((index + 1) % images.length)} className="absolute right-6 top-1/2 rounded-full bg-background/80 p-2"><ChevronRight /></button><div className="flex justify-center gap-2 pt-3">{images.map((image, i) => <button key={image} aria-label={`Show image ${i + 1}`} onClick={() => setIndex(i)} className={`size-2 rounded-full ${i === index ? 'bg-primary' : 'bg-muted-foreground/40'}`} />)}</div></>}</div> }
